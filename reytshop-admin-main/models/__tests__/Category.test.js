import mongoose from "mongoose";
import { Category } from "../Category.js"; // Use .mjs here

describe("Category Model Validation", () => {
  test("sample test case for category", () => {
    expect(true).toBe(true);
  });
});
